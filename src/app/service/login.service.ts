import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class LoginService {

  // private loginaApiUrl!: "http://localhost:8081/api/v1/employee/login";

  // constructor(
  //   private httpClient: HttpClient,
  // ) { }

  // login(newLogin:Login):Observable<Login>{
  //   return this.httpClient.post<Login>(this.loginaApiUrl,newLogin);
  //  }


}
